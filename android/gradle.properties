# JVM 参数：中文字符与内存设置
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX（本项目未引用第三方库，仅为兼容保留）
android.useAndroidX=true
android.nonTransitiveRClass=true

# 关闭构建缓存以降低磁盘占用
org.gradle.caching=false
